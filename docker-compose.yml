services:
  web:
    build:
      context: .
      target: runtime
    env_file: .env
    ports:
      - '3000:3000'
    restart: unless-stopped

  worker:
    container_name: worker
    build:
      context: .
      target: worker
    env_file: .env
    restart: unless-stopped
